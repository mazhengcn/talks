services:
  talks:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - ..:/talks:cached
      - node_modules:/talks/node_modules
    env_file: .env
    command: "sleep infinity"

volumes:
  node_modules:
